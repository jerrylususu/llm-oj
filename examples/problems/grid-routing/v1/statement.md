# Grid Routing

给定一个二维网格，提交程序需要输出一条从 `S` 到 `G` 的路径，移动过程中不能越界，也不能撞上障碍物 `#`。

## 输入输出约定

- 提交格式：`Python zip project`
- 入口文件：`main.py`
- 运行方式：`python main.py '<json-payload>'`
- 输入 JSON 字段：
  - `instance_id`：样例编号，仅用于调试
  - `grid`：字符串数组，`S` 是起点，`G` 是终点，`.` 是可通行格，`#` 是障碍物
- 标准输出：单行路径字符串，只能包含 `U`、`D`、`L`、`R`

示例：

```json
{
  "instance_id": "demo",
  "grid": [
    "S....",
    ".###.",
    "...#.",
    ".#...",
    "...#G"
  ]
}
```

一种合法输出：

```text
RRRRDDDD
```

## 评测规则

每个 case 的分数范围都是 `0..1`。

1. 如果路径越界、撞障碍、输出非法字符，或者最终没有到达 `G`，该 case 记 `0` 分。
2. 如果路径合法且到达终点，case 分数由四部分组成：
   - `0.60`：完成奖励
   - `0.20 * efficiency`
   - `0.12 * turn_bonus`
   - `0.08 * straight_bonus`

其中：

- `efficiency = 参考路径长度 / 你的路径长度`
- `turn_bonus = min((参考转弯数 + 1) / (你的转弯数 + 1), 1)`
- `straight_bonus = min(你的最长连续直行段 / 参考最长连续直行段, 1)`

说明：

- 参考路径由出题方预先设定，用来定义“足够短、转弯较少、路线较直”的目标。
- 更长但更直的路径仍可能拿到一部分附加分。
- `shown` 返回逐 case 结果与消息，`hidden` / `heldout` 只返回聚合得分。

## 题目意图

这个样例用来展示 `scorer` 不只是判对错，也可以做更细粒度的结构化打分：

- 基础合法性
- 是否真正到达目标
- 步数效率
- 转弯次数
- 路线平直度
