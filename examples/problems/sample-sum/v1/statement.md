# Sample Sum

给定两个整数 `a` 和 `b`，提交程序需要输出它们的和。

## 提交约定

- 提交格式：`Python zip project`
- 入口文件：`main.py`
- 运行方式：`python main.py '{"a":1,"b":2}'`
- 标准输出：单行整数结果，例如 `3`

## 评测语义

- `shown`：返回逐 case 结果
- `hidden`：只返回聚合得分
- `heldout`：仅在 `official` 模式运行
